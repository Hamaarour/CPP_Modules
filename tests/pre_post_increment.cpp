int main() {
    int x = 5;
    int y = ++x; // Pre-increment: x is incremented, y gets the new value of x (6).

    int a = 10;
    int b = a++; // Post-increment: b gets the current value of a (10), a is incremented (a becomes 11).

    return 0;
}